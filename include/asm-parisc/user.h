multiline_comment|/* This file should not exist, but lots of generic code still includes&n;   it. It&squot;s a hangover from old a.out days and the traditional core&n;   dump format.  We are ELF-only, and so are our core dumps.  If we&n;   need to support HP/UX core format then we&squot;ll do it here&n;   eventually. */
eof
